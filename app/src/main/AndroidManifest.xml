<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DoAnCuoiKi"
        tools:targetApi="31">
        <activity
            android:name=".DangNhapActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".TrangChuActivity" />
        <activity android:name=".EmployeeManagementActivity" />
        <activity android:name=".CheckInActivity"/>
        <activity android:name=".DatPhongActivity"/>
        <activity android:name=".InvoiceActivity"/>
        <activity android:name=".AdapterDanhSachNhanPhong.DailyCheckInsActivity"/>
        <activity android:name=".AdapterDanhSachNhanVien.EmployeeListActivity"/>
        <activity android:name=".AdapterDoanhThu.RevenueStatisticsActivity"/>
    </application>

</manifest>